---
- name: Provisioning of the raspberry pi 4 server
  hosts: pi
  become: yes
  gather_facts: yes
  roles:
    - device_init
    - install_packages
    - firewalld
    - prometheus_node_exporter
    - nfs_share
    - systemd_timesyncd
    - docker
    - nomad

  # Prometheus, syncthing, haproxy
