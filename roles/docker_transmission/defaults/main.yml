---
firewalld_zone: "{{ host_firewalld_zone }}"
transmission_ports: "{{ host_docker_transmission_ports }}"
transmission_firewalld_ports: "{{ host_docker_transmission_firewalld_ports }}"
transmission_directories: "{{ host_docker_transmission_directories }}"
transmission_volumes: "{{ host_docker_transmission_volumes }}"
transmission_openvpn_provider: "{{ host_docker_transmission_openvpn_provider }}"
transmission_openvpn_config: "{{ host_docker_transmission_openvpn_config }}"
transmission_openvpn_username: "{{ host_docker_transmission_openvpn_username }}"
transmission_openvpn_password: "{{ host_docker_transmission_openvpn_password }}"
transmission_image_version: "{{ host_docker_transmission_image_version }}"