---
unbound_interfaces_ip:
  - "{{ grp_device.router.ipv4_addr }}"
  - "127.0.0.1"

unbound_settings: "{{ host_unbound_settings }}"
unbound_forward_zone: "{{ host_unbound_forward_zone }}"
unbound_acces_control: "{{ host_unbound_acces_control }}"
unbound_local_zone: "{{ host_unbound_local_zone }}"
unbound_static_subdomain: "{{ host_unbound_static_subdomain }}"