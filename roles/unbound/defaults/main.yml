---
unbound_interfaces_ip:
  - "{{ _device.router.ipv4_addr }}"
  - "127.0.0.1"

unbound_settings: "{{ _unbound_settings }}"
unbound_forward_zone: "{{ _unbound_forward_zone }}"
unbound_acces_control: "{{ _unbound_acces_control }}"
unbound_local_zone: "{{ _unbound_local_zone }}"
unbound__static_subdomain: "{{ _unbound__static_subdomain }}"