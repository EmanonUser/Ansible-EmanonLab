---
primary_user_username: "{{ v_host_primary_user_username }}"

acme_directory: "{{ host_acme_directory }}"
acme_version: "{{ host_acme_version }}"
acme_challenge_type:  "{{ host_acme_challenge_type }}"
acme_email: "{{ host_acme_email }}"
acme_letsencrypt_base_path: "{{ host_acme_letsencrypt_base_path }}"
acme_letsencrypt_dirs: "{{ host_acme_letsencrypt_dirs }}"
acme_domain_name: "{{ host_acme_domain_name }}"
acme_http_server_path:  "{{ host_acme_http_server_path }}"
acme_server_port: "{{ host_acme_server_port }}"