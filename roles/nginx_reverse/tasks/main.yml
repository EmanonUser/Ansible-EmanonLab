---
- name: Install nginx from debian stable
  ansible.builtin.apt:
    name: nginx
    state: present
  
- name: Configure reverse proxy(s)
  ansible.builtin.template:
    src: reverse_template.conf.j2
    dest: "/etc/nginx/conf.d/podman.conf"
    owner: root
    group: root
    mode: u=rw,g=,o=
