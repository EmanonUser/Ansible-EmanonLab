---
jellyfin_ports: "{{ host_podman_jellyfin_ports }}"
jellyfin_volumes: "{{ host_podman_jellyfin_volumes }}"
firewalld_zone: "{{ host_firewalld_zone }}"
podman_firewalld_ports: "{{ host_podman_firewalld_ports }}"