---
- name: Install ntpd package
  ansible.builtin.apt:
    name: ntp
    state: present
  notify: Enable ntp service

- name: Configure ntpd
  ansible.builtin.template:
    src: ntp.conf.j2
    dest: /etc/ntp.conf
    owner: root
    group: root
    mode: u=rw,g=r,o=r
  notify: Start ntp service