---
- name: Configuration of systemd-timesyncd
  ansible.builtin.template:
    src: timesyncd.conf.j2
    dest: /etc/systemd/timesynd.conf
    owner: root
    group: root
    mode: u=rw,g=r,o=r
  notify: Enable & Restart systemd-timesyncd service