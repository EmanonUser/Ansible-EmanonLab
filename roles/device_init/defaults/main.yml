---
primary_user_ssh_pubkey: "{{ grp_primary_user_ssh_pubkey }}"
primary_user_username: "{{ host_primary_user_username }}"
primary_user_password: "{{ host_primary_user_password }}" 
debian_stable_packages: "{{ host_device_init_debian_stable_packages }}"
use_starship: "{{ host_device_init_use_starship }}"