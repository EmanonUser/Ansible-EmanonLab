---
primary_user_ssh_pubkey: "{{ _primary_user_ssh_pubkey }}"
create_user: "{{ _create_user }}"

# host_vars vault.yml
primary_user_username: "{{ _primary_user_username }}"
primary_user_password: "{{ _primary_user_password }}" 