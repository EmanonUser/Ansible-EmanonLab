---
firewalld_zone: "{{ host_firewalld_zone }}"
grafana_ports: "{{ host_docker_grafana_ports }}"
grafana_firewalld_ports: "{{ host_docker_grafana_firewalld_ports }}"
grafana_url: "{{ host_grafana_url }}"
grafana_admin_password: "{{ v_host_grafana_admin_password }}"