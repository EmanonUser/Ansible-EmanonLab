---
- name: Install nfs client
  ansible.builtin.apt:
    name: nfs-common
    state: present

- name: Configure nfs share
  ansible.builtin.template:
    src: mnt-vault.mount.j2
    dest: /etc/systemd/system/mnt-vault.mount
    owner: root
    group: root
    mode: u=rw,g=r,o=r
  notify:
    - Daemon-reload nfs_share systemd
    - Enable mnt-vault.mount service
    - Start mnt-vault.mount service
    - Restart mnt-vault.mount service