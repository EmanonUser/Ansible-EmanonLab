---
WAN_interface_name: "{{ _WAN_interface_name }}"
LAN_interface_1_name: "{{ _LAN_interface_name[0] }}"
LAN_interface_2_name: "{{ _LAN_interface_name[1] }}"

LAN_static_ip: "{{ _device.router.ipv4_addr }}"
LAN_static_ip_cidr: "{{ _device.router.ipv4_addr_cidr }}"
LACP_interface_name: "{{ _LACP_interface_name }}"