---
- name: Install and configure sshguard
  block:

  - name: Install sshguard
    ansible.builtin.apt:
      name: sshguard
      default_release: buster-backports
      state: present
    notify: Enable & Restart sshguard service