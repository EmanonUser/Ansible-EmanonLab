---
firewalld_zone: "{{ host_firewalld_zone }}"
prometheus_ports: "{{ host_docker_prometheus_ports }}"
prometheus_firewalld_ports: "{{ host_docker_prometheus_firewalld_ports }}"
prometheus_volumes: "{{ host_docker_prometheus_volumes }}"
prometheus_volumes_path: "{{ host_docker_prometheus_volumes_path }}"
prometheus_config_path: "{{ host_docker_prometheus_config_path }}"
prometheus_image_version: "{{ host_docker_prometheus_image_version }}"