---
haproxy_firewalld_zone: "{{ host_firewalld_zone | default('home') }}"
haproxy_firewalld_services: ["http", "https"]
haproxy_stats: "{{ host_haproxy_stats | default(true) }}"
haproxy_backend_maxconn: 30