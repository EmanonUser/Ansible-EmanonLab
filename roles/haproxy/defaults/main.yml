---
haproxy_firewalld_zone: "{{ host_haproxy_firewalld_zone }}"
haproxy_firewalld_services: "{{ host_haproxy_firewalld_services }}"
podman_networking: "{{ grp_podman_networking }}"
haproxy_acme_server_port: "{{ host_haproxy_acme_server_port }}"
haproxy_stats: "{{ host_haproxy_stats }}"
haproxy_stats_port: "{{ host_haproxy_stats_port }}"