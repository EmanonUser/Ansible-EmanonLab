---
- name: Enable haproxy service
  ansible.builtin.systemd:
    name: haproxy.service
    enabled: yes

- name: Reload haproxy service
  ansible.builtin.systemd:
    name: haproxy.service
    state: reloaded