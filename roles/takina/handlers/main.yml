---
- name: Daemon-reload takina systemd
  ansible.builtin.systemd:
    daemon_reload: yes

- name: Enable takina timer
  ansible.builtin.systemd:
    name: takina.timer
    enabled: yes

- name: Start takina timer
  ansible.builtin.systemd:
    name: takina.timer
    state: started