---
primary_user_username: "{{ v_host_primary_user_username }}"
gandi_api_key: "{{ grp_gandi_api_key }}"
gandi_domain_name: "{{ grp_domain_name }}"
gandi_subdomain: "{{ grp_subdomain }}"
gad_path: "{{ host_gad_path }}"
gad_version: "{{ host_gad_version }}"
gad_use_ipv6: "{{ host_gad_use_ipv6 }}"