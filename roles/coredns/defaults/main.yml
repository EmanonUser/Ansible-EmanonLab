---
cpu_architecture: "{{ host_coredns_cpu_architecture }}"
coreDNS_path: "{{ host_coreDNS_path }}"
coreDNS_version: "{{ host_coreDNS_version }}"
coreDNS_ip: "{{ grp_device.router.ipv4_addr }}"
coreDNS_port: "{{ host_coreDNS_port }}"
coreDNS_prometheus_port: "{{ host_coreDNS_prometheus_port }}"
coreDNS_docker_local_subdomain: "{{ host_coreDNS_docker_local_subdomain }}"
coreDNS_forwardto: "{{ host_coreDNS_forwardto }}"