(snip) {
    bind 127.0.0.1
    bind {{ coredns_ip }}
    any
    minimal
    log
    cache 300
    errors {
        consolidate 120s ".* i/o timeout$"
        consolidate 30s "^Failed to .+"
    }
}

local.emanon:{{ coredns_port }} {
    import snip
    prometheus {{ coredns_ip }}:{{ coredns_prometheus_port }}
    file /opt/coredns/master.db
}

.:{{ coredns_port }} {
    import snip
    prometheus {{ coredns_ip }}:{{ coredns_prometheus_port }}
    forward . {{ coredns_forwardto }}:10053 {
        except example.org
        except local.emanon
        except in-addr.arpa
        health_check 5s
        prefer_udp
    }
}