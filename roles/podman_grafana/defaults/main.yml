---
primary_user_username: "{{ host_podman_grafana_roleless_primary_user_username }}"
podman_grafana_image: "{{ host_podman_grafana_image }}"
podman_grafana_image_version: "{{ host_podman_grafana_image_version }}"
podman_grafana_ports: "{{ host_podman_grafana_ports }}"
podman_grafana_firewalld_ports: "{{ host_podman_grafana_firewalld_ports }}"
podman_grafana_url: "{{ host_podman_grafana_url }}"
podman_grafana_admin_password: "{{ host_podman_grafana_admin_password }}"
podman_grafana_dashboards_path: "{{ host_podman_grafana_dashboards_path }}"
podman_grafana_firewalld_zone: "{{ host_podman_flame_firewalld_zone }}"
podman_networking: "{{ grp_podman_networking }}"