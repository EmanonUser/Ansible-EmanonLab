---
zfs_pools: "{{ host_zfs_pools }}"
zfs_datasets_keylocation: "{{ host_zfs_datasets_keylocation | default('/root/zfs_datasets_keys') }}"
zfs_volumes_keylocation: "{{ host_zfs_volumes_keylocation | default('/root/zfs_volumes_keys') }}"
systemd_on_calendar_scrubing_timer: "{{ host_zfs_systemd_on_calendar_scrubing_timer | default('Sat 00:00:00') }}"
zfs_arc_max: "{{ host_zfs_arc_max | default('2147483648') }}" # At least 2 Gibibytes
zfs_manual_zpools_creation: "{{ host_zfs_manual_zpools_creation | default(false) }}"