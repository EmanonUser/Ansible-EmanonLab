---
zfs_pools: "{{ host_zfs_pools }}"
zfs_datasets_keylocation: "{{ host_zfs_datasets_keylocation }}"
zfs_volumes_keylocation: "{{ host_zfs_volumes_keylocation }}"
systemd_on_calendar_scrubing_timer: "{{ host_zfs_systemd_on_calendar_scrubing_timer }}"
zfs_arc_max: "{{ host_zfs_arc_max }}"
zfs_manual_zpools_creation: "{{ host_zfs_manual_zpools_creation }}"