---
primary_user_username: "{{ host_podman_transmission_roleless_primary_user_username }}"
podman_transmission_image: "{{ host_podman_transmission_image }}"
podman_transmission_image_version: "{{ host_podman_transmission_image_version }}"
podman_transmission_ports: "{{ host_podman_transmission_ports }}"
podman_transmission_firewalld_ports: "{{ host_podman_transmission_firewalld_ports }}"
podman_media_path: "{{ host_podman_media_path }}"
podman_transmission_directories: "{{ host_podman_transmission_directories }}"
podman_transmission_volumes: "{{ host_podman_transmission_volumes }}"
podman_transmission_openvpn_provider: "{{ host_podman_transmission_env_openvpn_provider }}"
podman_transmission_openvpn_config: "{{ host_podman_transmission_env_openvpn_config }}"
podman_transmission_openvpn_username: "{{ host_podman_transmission_env_openvpn_username }}"
podman_transmission_openvpn_password: "{{ host_podman_transmission_env_openvpn_password }}"
podman_transmission_firewalld_zone: "{{ host_podman_transmission_firewalld_zone }}"
podman_networking: "{{ grp_podman_networking }}"