---
- name: Enable podman service
  ansible.builtin.systemd:
    name: podman.service
    enabled: yes

- name: Start podman service
  ansible.builtin.systemd:
    name: podman.service
    state: started