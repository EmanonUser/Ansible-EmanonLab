---
- name: Enable syncthing_ansible podman service
  ansible.builtin.systemd:
    name: podman-syncthing_ansible.service
    enabled: true
    scope: user
  become_user: "{{ primary_user_username }}"
