---
- name: Enable transmission_ansible podman service
  ansible.builtin.systemd:
    name: podman-transmission_ansible.service
    enabled: true
    scope: user
  become_user: "{{ primary_user_username }}"
