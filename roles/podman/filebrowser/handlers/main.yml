---
- name: Enable filebrowser_ansible podman service
  ansible.builtin.systemd:
    name: podman-filebrowser_ansible.service
    enabled: true
    scope: user
  become_user: "{{ primary_user_username }}"
