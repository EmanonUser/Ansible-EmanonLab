---
- name: Create loki volume directory
  ansible.builtin.file:
    path: "{{ item }}"
    owner: emanon
    group: emanon
    mode: u=rwx,g=,o=
    state: directory
  loop: "{{ podman_loki_directories }}"

- name: Run loki podman container
  containers.podman.podman_container:
    name: loki_ansible
    image: "{{ podman_loki_image }}:{{ podman_loki_image_version }}"
    publish: "{{ podman_loki_ports }}"
    volume: "{{ podman_loki_volumes }}"
    restart_policy: unless-stopped
    state: started
    command:
      - "-config.file=/mnt/config/loki-config.yaml"
    generate_systemd:
      container_prefix: podman
      path: /home/{{ primary_user_username }}/.config/systemd/user/
  become_user: "{{ primary_user_username }}"
  notify: Enable loki_ansible podman service

- name: Open loki firewalld ports
  ansible.posix.firewalld:
    zone: "{{ firewalld_zone }}"
    port: "{{ podman_loki_firewalld_ports }}"
    permanent: yes
    immediate: yes
    state: enabled
  when: grp_podman_networking.loki.lan_access

- name: Close loki firewalld ports
  ansible.posix.firewalld:
    zone: "{{ firewalld_zone }}"
    port: "{{ podman_loki_firewalld_ports }}"
    permanent: yes
    immediate: yes
    state: disabled
  when: not grp_podman_networking.loki.lan_access
