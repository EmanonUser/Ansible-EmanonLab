---
primary_user_username: "{{ host_podman_jellyfin_roleless_primary_user_username }}"
podman_jellyfin_image: "{{ host_podman_jellyfin_image }}"
podman_jellyfin_image_version: "{{ host_podman_jellyfin_image_version }}"
podman_jellyfin_ports: "{{ host_podman_jellyfin_ports }}"
podman_jellyfin_volumes: "{{ host_podman_jellyfin_volumes }}"
podman_jellyfin_directories: "{{ host_podman_jellyfin_directories }}"
podman_jellyfin_firewalld_zone: "{{ host_podman_jellyfin_firewalld_zone }}"
podman_jellyfin_firewalld_rich_rules: "{{ host_podman_jellyfin_firewalld_rich_rules }}"
podman_networking: "{{ grp_podman_networking }}"