---
- name: Enable prometheus_ansible podman service
  ansible.builtin.systemd:
    name: podman-prometheus_ansible.service
    enabled: true
    scope: user
  become_user: "{{ primary_user_username }}"
