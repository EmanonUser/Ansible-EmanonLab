---
WAN_interface_name: "{{ grp_device.router.wan_interface }}"
wireguard_interface_name: "{{ host_nftables_wireguard_interface_name }}"
wireguard_subnet: "{{ host_nftables_wireguard_subnet }}"
wireguard_server_port: "{{ host_nftables_wireguard_server_port }}"

arch_ipv4_addr: "{{ grp_device.arch.ipv4_addr }}"
pi_ipv4_addr: "{{ grp_device.pi.ipv4_addr }}"
brocade_ipv4_addr: "{{ grp_device.brocade.ipv4_addr }}"

domain_name: "{{ grp_domain_name }}"
subdomain_name: "{{ grp_subdomain }}"
router_static_ip: "{{ grp_device.router.ipv4_addr }}"

lacp_interface_name: "{{ host_interfaces_lacp_interface }}"
echo_request_count: "{{ host_interfaces_is_alive_echo_request_count }}"