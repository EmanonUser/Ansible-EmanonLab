---
cpu_architecture: "{{ host_cpu_architecture | default('amd64') }}"
node_exporter_version: "{{ host_node_exporter_version | default('1.3.1') }}"
# https://github.com/prometheus/node_exporter/releases/

node_exporter_interface_addr: "{{ host_node_exporter_interface_addr | default('0.0.0.0') }}"
node_exporter_port: "{{ host_node_exporter_port | default('9100') }}"