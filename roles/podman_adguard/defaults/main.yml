---
primary_user_username: "{{ host_podman_adguard_roleless_primary_user_username }}"
podman_adguard_image: "{{ host_podman_adguard_image }}"
podman_adguard_image_version: "{{ host_podman_adguard_image_version }}"
podman_adguard_ports: "{{ host_podman_adguard_ports }}"
podman_adguard_firewalld_ports: "{{ host_podman_adguard_firewalld_ports }}"
podman_adguard_directories: "{{ host_podman_adguard_directories }}"
podman_adguard_volumes: "{{ host_podman_adguard_volumes }}"
podman_firewalld_zone: "{{ host_podman_firewalld_zone }}"
podman_networking: "{{ grp_podman_networking }}"