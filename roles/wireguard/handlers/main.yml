---
- name: Enable & Restart wireguard service
  ansible.builtin.systemd:
    name: wg-quick@{{ wireguard_interface_name }}.service
    enabled: yes
    name: wg-quick@{{ wireguard_interface_name }}.service
