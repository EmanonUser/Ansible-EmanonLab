---
- name: Provisioning of the main Debian router
  hosts: router
  become: yes
  gather_facts: yes
  roles:
    - role: device_init
      tags: [ device_init, init ]

    - role: fstrim
      tags: [ fstrim, init ]

    - role: interfaces
      tags: [ interfaces, init ]

    - role: vnstat
      tags: [ vnstat, init, monitoring ]

    - role: starship
      tags: [ starship, init ]

    - role: frr
      tags: [ frr, services ]

    - role: kea
      tags: [ kea, services ]

    # - role: stork_agent
    #   tags: [ stork_agent, services ]

    - role: coredns
      tags: [ coredns, services ]

    - role: wireguard
      tags: [ wireguard, services ]

    # - role: ntpd
    #   tags: [ ntpd, services ]

    - role: sshguard
      tags: [ sshguard, services ]

    - role: nftables
      tags: [ nftables, services ]