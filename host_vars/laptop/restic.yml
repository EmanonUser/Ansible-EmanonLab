---
host_restic_repos:
  - repo: minecraft
    path: /home/restic/minecraft
    password: "{{ vhost_restic_minecraft_password }}"

host_restic_tasks:
  - task: minecraft
    dest_repo: minecraft
    need_read_privilege: yes
    systemd_timer_interval: 30m
    files_backup:
      - /tank/minecraft_02