---
restic_repos:
  - repo: minecraft
    path: /home/restic/minecraft
    password: "{{ vhost_restic_minecraft_password }}"
    tasks:
      - task: minecraft
        need_read_privilege: yes
        systemd_timer_interval: 6h
        files_backup:
          - /tank/minecraft