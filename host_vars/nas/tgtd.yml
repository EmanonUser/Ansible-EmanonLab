---
host_iscsi_targets:
  - iqn: iqn.2022-02.local.srv:zvol
    backing_stores:
      - backing_store: /dev/zvol/vault/zvol_01
    incoming_users: "{{ v_tgtd_incoming_users.iqn_2022_02_local_srv_zvol }}"
    initiator_addresses:
      - initiator_address: "{{ grp_device.arch.ipv6_addr }}"
      - initiator_address: "{{ grp_device.arch.ipv4_addr }}"

  - iqn: iqn.2022-02.local.nas:zvol-02
    backing_stores:
      - backing_store: /dev/zvol/vault/zvol_02
    incoming_users: "{{ v_tgtd_incoming_users.iqn_2022_02_local_srv_zvol }}"
    initiator_addresses:
      - initiator_address: "{{ grp_device.arch.ipv6_addr }}"
      - initiator_address: "{{ grp_device.arch.ipv4_addr }}"
