---
- name: Provisioning of the laptop server
  hosts: laptop
  become: yes
  gather_facts: yes
  roles:
    - device_init
    - firewalld
    - podman
    - systemd_timesyncd
    - is_alive
    - gad
    - zfs
    - zrepl
    - podman_jellyfin
    - podman_transmission
    - podman_filebrowser
    - haproxy
    - acme
    - gad


# sudo zpool create vault /home/emanon/a.zfs; sudo zfs create vault/files; sudo zfs create vault/media; sudo zfs create vault/data; sudo chown emanon:emanon #-R /vault
