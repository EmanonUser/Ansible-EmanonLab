---
- name: Provisioning of the laptop server
  hosts: laptop 
  become: yes
  gather_facts: yes
  roles:
    - device_init
    - install_packages
    - kernel_upgrade
    - systemd_timesyncd
    - is_alive
    - zfs
    - zrepl